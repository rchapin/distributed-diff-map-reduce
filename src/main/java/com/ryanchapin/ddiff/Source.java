package com.ryanchapin.ddiff;

/**
 * Enum to indicate from which set of input files, the reference data or the
 * test data, that the record belongs.
 * 
 * @since  1.0.0
 *
 */
public enum Source {
   REFERENCE,
   TEST;
}
